/// Пороги ширины экрана (логические пиксели) для адаптивной вёрстки.
/// Соответствуют рекомендациям Material Design 3.
abstract class Breakpoints {
  /// До этой ширины считаем экран «узким» (телефон, портрет).
  /// От 0 до [tablet] — одна колонка, компактный UI.
  static const double tablet = 600;

  /// От [tablet] до [desktop] — планшет / телефон в ландшафте.
  /// Можно использовать две колонки, боковые панели.
  static const double desktop = 840;

  /// Выше [desktop] — десктоп: несколько колонок, максимальная ширина контента и т.д.
}
